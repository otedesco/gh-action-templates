{
  "name": "workflow-fixture-uncovered-source",
  "private": true,
  "scripts": {
    "format:check": "node gate.mjs format",
    "lint:check": "node gate.mjs lint",
    "type:check": "node gate.mjs type",
    "test": "node gate.mjs test",
    "test:coverage": "node gate.mjs coverage",
    "build": "node gate.mjs build",
    "quality:check": "pnpm run format:check && pnpm run lint:check && pnpm run type:check && pnpm run test && pnpm run test:coverage && pnpm run build"
  }
}
