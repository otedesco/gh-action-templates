name: Container release
on:
  workflow_call:
    secrets:
      NPM_TOKEN:
        required: true
permissions: {}
jobs:
  release:
    permissions:
      contents: read
      packages: write
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
    uses: example/release-container.yml@immutable
