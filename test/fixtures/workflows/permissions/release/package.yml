name: Package release
on:
  workflow_call:
    secrets:
      NPM_TOKEN:
        required: true
permissions: {}
jobs:
  release:
    permissions:
      contents: write
      pull-requests: write
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
    uses: example/release-package.yml@immutable
