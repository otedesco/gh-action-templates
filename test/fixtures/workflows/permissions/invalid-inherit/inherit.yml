name: Inherited secrets
on:
  workflow_call:
permissions:
  contents: read
  actions: write
jobs:
  quality:
    permissions:
      contents: read
      actions: write
    uses: example/reusable.yml@main
    secrets: inherit
